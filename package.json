{
  "name": "flowstate",
  "version": "1.0.0",
  "description": "A productivity application to optimize time management, task management, and focus, with specific features for users with ADHD.",
  "scripts": {
    "start": "npm run dev",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend",
    "install-server": "npm install --prefix backend",
    "install-client": "npm install --prefix frontend"
  },
  "keywords": [
    "productivity",
    "task management",
    "time management",
    "ADHD"
  ],
  "author": "Bolt",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.0",
    "@mui/material": "^6.4.4"
  }
}
